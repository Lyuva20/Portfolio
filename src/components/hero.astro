


<section class="hero__contenedor">

    <h2 class="hero__titulo">Sobre mí 😅</h2>

    <p class="hero__texto">
        Soy estudiante de desarrollo web con experiencia en romper y arreglar el mismo código cinco veces.. Empecé con HTML pensando que todo sería fácil,
        pero luego llegaron CSS, JavaScript y las crisis existenciales.
    </p>

</section>

<style>

.hero__contenedor {

    max-width: 900px;
    margin: 80px auto;
    padding: clamp(16px, 4vw, 48px);
    background: var(--color_fondo);
    background-blend-mode: soft-light;
    box-shadow: var(--contenedor_sombras);
    border-radius: 20px;

}

.hero__titulo {

    font-family: var(--font_titulo);
    color: var(--color_titulo);
    font-size: 2.4rem;
    text-align: center;
    margin-bottom: 1.5rem;

}

.hero__texto {

    font-family: var(--font_texto);
    color: var(--color_subtitulo);
    font-size: 1.1rem;
    line-height: 1.7;
    opacity: 0.9;
    text-align: justify;
    
}


</style>
