---
import {Image} from "astro:assets";
    import cara from "../images/cara.jpg"
---

<section class="header__contenedor"> 


    <div>
        <h1 class="header__titulo">¡Hola, soy Osmi! <span>👋</span> </h1>
        <p class="header__parrafo">Bienvenido a mi humilde portfolio.</p>
    </div>
    
    <Image class="header__foto" src={cara} alt="Fondo caricatura"/>

</section>

<style>

.header__contenedor{

    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: clamp(10px,4vw,20px); 
    /*Tamaño minimo/ tamaño ideal proporcional/ tamaño maximo*/
    align-items: center;
    max-width: 950px;
    margin: auto;
    margin-top: 50px;
    padding: clamp(16px,4vw,48px);
    background: var(--color_principal);
    background-blend-mode: soft-light;
    box-shadow: var(--contenedor_sombras);
    border-radius: 20px;
    
}

.header__titulo{

    font-family: var(--font_titulo);
    color: var(--color_titulo);
    font-size: 2.5rem;
    font-weight: bold;
    text-align: center;
    margin-left: 20px;

}



.header__parrafo{

    font-family: var(--font_texto);
    color: var(--color_subtitulo);
    margin-top: 2.5rem;
    opacity: .85;
    font-size: 2rem;
    margin-left: 10px;


}

.header__foto{

    justify-self: center;
    border-radius: 50%;
    object-fit: cover;
    box-shadow: 0 10px 25px var(--bordes);  

}



</style>